import { Routes } from '@angular/router';
import { ProductAddComponent } from './product-add.component';

export const product_add_routes: Routes = [
  {
    path: '',
    component: ProductAddComponent
  }
];
