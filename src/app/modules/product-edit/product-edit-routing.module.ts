import { Routes } from '@angular/router';
import { ProductEditComponent } from './product-edit.component';

export const product_edit_routes: Routes = [
  {
    path: '',
    component: ProductEditComponent
  }
];
